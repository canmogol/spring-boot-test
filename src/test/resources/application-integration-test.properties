# JDBC URL of the database.
spring.datasource.url=jdbc:h2:mem:test
# Additional native properties to set on the JPA provider. 'properties' field is a Map<String, String>.
# 'hibernate.dialect' is a shortcut for setting the corresponding property.
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect